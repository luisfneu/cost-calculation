[pytest]
testpaths = tests
# Garante a raiz do projeto no sys.path (para `import app`) mesmo quando o
# pytest é chamado direto (ex.: no CI), não só via `python -m pytest`.
pythonpath = .
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
